# Modifier Definitions

Modifiers are the authored definition tree for configurable choices.

The tree contains prompts, their choices, validation rules, schedules, effects, prices, and optional nested modifiers. It describes what may be selected; it is not itself the state of a particular selection.

Hydration applies selection input and context to this tree to produce a validated configuration.
