# Modifier Definitions

Modifiers are the authored definition tree for configurable choices.

The tree contains prompts, their choices, validation rules, schedules, effects, prices, optional choice-owned text inputs, and optional nested modifiers. It describes what may be selected and entered; it is not itself the state of a particular configuration.

Hydration applies selection input and context to this tree to produce a validated configuration.
