# Typed ID

A typed ID is a public domain identifier whose prefix identifies the kind of object it names.

The shared prefixed-ID value validates the prefix, separator, and suffix. Domain-specific ID wrappers fix the expected prefix, preventing an ID for one kind of object from being parsed as another kind.

The prefix is part of the serialized identifier, not merely display formatting.
