# UTC Time

UTC time is a global instant represented as signed Unix milliseconds.

It can convert to and from the standard library's system time on either side of the Unix epoch and exposes checked millisecond arithmetic. It does not carry a time zone, local calendar fields, or an entity ID.
