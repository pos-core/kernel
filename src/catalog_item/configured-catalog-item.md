# Configured Catalog Item

A configured catalog item is the validated result of applying a variant choice and modifier selections to a catalog item.

It contains the selected IDs, resolved labels, variant effects, invariant price, hydrated modifier configuration, modifier pricing, and combined total price. Consumer profile and evaluation time are applied while producing this value when the caller supplies them.

This is configuration output. An order item copies the facts it needs into order-owned snapshot types.
